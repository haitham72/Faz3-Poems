# Audio Speaker Recognition System - Dependencies

# Core speaker recognition
resemblyzer==0.1.1.dev0
torch>=2.0.0
torchaudio>=2.0.0

# Audio/video processing
pydub>=0.25.1
ffmpeg-python>=0.2.0
librosa>=0.10.0
soundfile>=0.12.0
noisereduce>=2.0.0

# Data processing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0

# Optional (for advanced VAD - may have build issues on Windows)
# webrtcvad-wheels>=2.0.10
# If webrtcvad fails to install, the system will use energy-based silence detection
